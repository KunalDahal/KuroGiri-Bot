services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bot
    restart: unless-stopped
    env_file: .env
    networks:
      - mongodb_barusu-net 

networks:
  mongodb_barusu-net:
    external: true